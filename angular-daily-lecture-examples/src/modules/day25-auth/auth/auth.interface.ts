export interface LoginCredential {
  email: string;
  password: string;
}

export interface AccessToken {
  acccessToken: string;
}

export interface User {
  name: string;
  email: string;
  id: string;
}
