export interface LoginCredential {
  email: string;
  password: string;
}

export interface AccessToken {
  accessToken: string;
}

export interface User {
  name: string;
  email: string;
  id: string;
}
